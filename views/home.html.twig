<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>Worse Yelp!</title>
    </head>
    <body>
        <div class="container">
            <h1>Restaurant Cuisine Matcher</h1>

            <h2>Add A Cuisine Type!</h2>
            <form action='/categories' method='post'>
                <label for='name'>Category name</label>
                <input id='name' name='name' type='text'>

                <button type='submit'>Add category</button>
            </form>
            <hr />

            <h2>Add A Restaurant To Our List!</h2>
            <form action='/categories' method='post'>
                <label for='name'>Category name</label>
                <input id='name' name='name' type='text'>
                <p>
                    What Cuisine Type is Featured Here?
                    <select name="restaurantCuisineType">
                        {% for cuisine in cuisines %}
                            <option value={{ cuisine.getId }}>{{ cuisine.getName }}</option>
                        {% endfor %}
                    </select>
                </p>
                <button type='submit'>Add category</button>
            </form>
            <hr />
            <div class="row">
                <div class="col-sm-4">
                    <h3>Cuisine Types</h3>
                    <hr>
                    <ul>
                    {% for cuisine in cuisines %}
                        <li><a href="/filter_by_cuisine/{{ cuisine.getName }}">{{ cuisine.getName }}</a></li><hr />
                    {% endfor %}
                    </ul>
                </div>
                <div class="col-sm-4">
                    <h3>Restaurants</h3>
                    <hr>
                    <ul>
                    {% for restaurant in restaurants %}
                        <li><a href="/filter_by_restaurant/{{ restaurant.getId }}">{{ restaurant.getName }}</a></li><hr />
                    {% endfor %}
                    </ul>
                </div>
                <div class="col-sm-2">
                    <h3>💲💲💲💲💲</h3>
                    <hr>
                    <ul>
                    {% for restaurant in restaurants %}
                        <li>
                            {% for i in 1.. restaurant.getCost %}
                            💲
                            {% endfor %}
                        </li><hr />
                    {% endfor %}
                    </ul>
                </div>
                <div class="col-sm-2">
                    <h3>🌟🌟🌟🌟🌟</h3>
                    <hr>
                    <ul>
                    {% for restaurant in restaurants %}
                        <li>
                            {% for i in 1.. restaurant.getRating %}
                            🌟
                            {% endfor %}
                        </li><hr />
                    {% endfor %}
                    </ul>
                </div>

            </div>




        </div>

    </body>
</html>
